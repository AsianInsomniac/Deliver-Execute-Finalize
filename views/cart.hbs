<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8"/>
        <title>Terra</title>
        <link href="https://fonts.googleapis.com/css?family=Playfair+Display&display=swap" rel="stylesheet" />
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
        <link href="/css/home.css" rel="stylesheet" />
        <link href="/css/navbar.css" rel="stylesheet" />
        <script src="/js/cart.js" type="text/javascript"></script>
    </head>

    <body>
        {{> navbar}}
            <div class="row " >
                <div id="cart">
                    {{!-- <div class="row mt-1 mb-4"> --}}
                        <div class="col-8 cart-title">Cart</div>
                        {{!-- <div class="col-4 ">
                            <a href="javascript:void(0)" class="closebtn" onclick="closeCart()">&times;</a>
                        </div> --}}
                    {{!-- </div> --}}
                        {{#if result}}
                        {{#each result}}
                        <div class="row d-flex justify-content-center mt-5 mb-5" id="cart-items">
                            <div class="row mt-1 mb-1 cart-product">
                                <div class="col-4">
                                    <img src="fb{{item}}.png" alt="product" class="product-picture">
                                </div>
                                <div class="col-8">
                                    <div class="product-name" id="item" name="item"><b>Name: </b> {{item}}</div>
                                    <div class="product-price" id="price" name="price"><b>Price: </b>{{price}}</div>
                                    <div class="product-quantity" id="qty" name="qty"><b>Qty: </b>{{qty}}</div>
                                </div>
                            </div>
                            {{"\n"~}}
                        {{/each}}
                        {{/if}}
                    <div class="row mt-5 mb-5 d-flex align-items-end" id="cart-checkout">
                        <div class="row d-flex align-items-end">
                            <div class="col">
                                <div class="total">Total:</div>
                            </div>
                            <div class="col">
                                <div class="money">{{price}}</div>
                            </div>
                        </div>
                        <div class="row d-flex align-items-end justify-content-center">
                            <a href="/checkout"><button class="checkout-btn">Checkout</button></a>
                        </div>
                    </div>
            </div>
            </div>
    </body>
</html>
